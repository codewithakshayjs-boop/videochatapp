import { useNavigate } from 'react-router-dom'; import { useChat } from '../context/ChatContext'; import { useLanguage } from '../context/LanguageContext'; import GenderSelector from '../components/GenderSelector'; import Button from '../components/Button';
export default function Profile() { const { profile, saveProfile } = useChat(); const { t } = useLanguage(); const navigate = useNavigate(); return <Setup title={t('aboutYou')} step="1 / 3"><p>{t('identityHelp')}</p><GenderSelector value={profile.gender} onChange={(gender) => saveProfile({ gender })} /><Button disabled={!profile.gender} onClick={() => navigate('/profile/dob')}>{t('continue')}</Button></Setup>; }
export function Setup({ title, step, children }) { return <main className="setup-page"><a href="/" className="brand small"><i>☕</i> CHAI<span>YO</span></a><section className="setup-card"><div className="progress"><span style={{ width: `${Number(step[0]) * 33.33}%` }} /><small>{step}</small></div><h1>{title}</h1>{children}</section></main>; }
