import { useNavigate } from 'react-router-dom'; import Button from '../components/Button'; import { useChat } from '../context/ChatContext';
export default function Home() { const navigate = useNavigate(); const { user, signOut, setChatMode } = useChat(); const chooseMode = (mode) => { setChatMode(mode); navigate(user ? '/searching' : '/profile'); }; return <main className="home chai-home"><nav><a className="brand" href="/"><i>☕</i> CHAI<span>YO</span></a><div className="nav-links"><a href="#safety">Our space</a><a href="#how">How it works</a></div>{user ? <><span className="welcome">Namaste, {user.name}</span><Button variant="ghost" onClick={signOut}>Sign out</Button><Button onClick={() => navigate('/searching')}>Find company</Button></> : <><Button variant="ghost" onClick={() => navigate('/auth/login')}>Sign in</Button><Button onClick={() => navigate('/profile')}>Meet someone</Button></>}</nav><section className="hero"><div className="hero-copy"><p className="eyebrow">GARAM CHAI • REAL TALKS • 18+</p><h1>Pour your heart out over <em>chai.</em></h1><p className="lede">CHAIYO is a warm little corner for honest conversations with someone new. Share a thought, a laugh, or what’s been on your heart.</p><div className="connect-options"><button onClick={() => chooseMode('text')}><span>⌁</span><strong>Text chat</strong><small>Message one-to-one</small></button><button onClick={() => chooseMode('audio')}><span>◉</span><strong>Audio call</strong><small>Talk voice-to-voice</small></button><button onClick={() => chooseMode('video')}><span>◔</span><strong>Video call</strong><small>Meet face-to-face</small></button></div><small>Be kind, keep it consensual, and confirm that you’re 18 or older.</small></div><div className="hero-art chai-art"><div className="floating-card one">CHAI TIME <span>♥</span></div><div className="orbital"><div className="person p1">☕</div><div className="person p2">♥</div><div className="connection" /></div><div className="floating-card two">A little warmth, one chat away</div></div></section><section id="how" className="steps"><div><b>01</b><h3>Choose your connection</h3><p>Pick text, audio, or video for your chai talk.</p></div><div><b>02</b><h3>Find chai company</h3><p>We’ll pair you with someone in the same mode.</p></div><div><b>03</b><h3>Speak from the heart</h3><p>Talk, listen, or move on whenever you want.</p></div></section></main>; }
