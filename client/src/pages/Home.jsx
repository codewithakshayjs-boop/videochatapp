import { useNavigate } from 'react-router-dom'; import Button from '../components/Button'; import { useChat } from '../context/ChatContext';
export default function Home() { const navigate = useNavigate(); const { user, signOut } = useChat(); return <main className="home"><nav><a className="brand" href="/"><i>◔</i> spark<span>link</span></a><div className="nav-links"><a href="#safety">Safety</a><a href="#how">How it works</a></div>{user ? <><span className="welcome">Hi, {user.name}</span><Button variant="ghost" onClick={signOut}>Sign out</Button><Button onClick={() => navigate('/searching')}>New chat</Button></> : <><Button variant="ghost" onClick={() => navigate('/auth/login')}>Sign in</Button><Button onClick={() => navigate('/profile')}>New chat</Button></>}</nav><section className="hero"><div className="hero-copy"><p className="eyebrow">PRIVATE • LIVE • 18+</p><h1>Meet someone new, <em>face to face.</em></h1><p className="lede">Spark a real conversation with someone anywhere in the world. Every chat starts fresh and stays between the two of you.</p><Button className="hero-button" onClick={() => navigate(user ? '/searching' : '/profile')}>Start video chat <b>→</b></Button><small>By continuing, you agree to our community rules and confirm you’re 18 or older.</small></div><div className="hero-art"><div className="floating-card one">LIVE <span>●</span></div><div className="orbital"><div className="person p1">✦</div><div className="person p2">●</div><div className="connection" /></div><div className="floating-card two">38,216 online</div></div></section><section id="how" className="steps"><div><b>01</b><h3>Set your profile</h3><p>Share only your first name, age and identity.</p></div><div><b>02</b><h3>Get matched</h3><p>We’ll find an available person in seconds.</p></div><div><b>03</b><h3>Start talking</h3><p>Skip or end a chat whenever you want.</p></div></section></main>; }
