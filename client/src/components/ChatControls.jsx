export default function ChatControls({ muted, cameraOff, flipping, onMute, onCamera, onFlip, onNext, onEnd }) { return <div className="chat-controls"><button onClick={onMute} aria-label="Toggle microphone">{muted ? '◉ Unmute' : '◉ Mute'}</button><button onClick={onCamera} aria-label="Toggle camera">{cameraOff ? '▣ Camera on' : '▣ Camera off'}</button><button onClick={onFlip} disabled={flipping} aria-label="Switch between front and back camera">{flipping ? '↻ Switching' : '↻ Flip'}</button><button className="next" onClick={onNext}>↻ Next</button><button className="end" onClick={onEnd}>End</button></div>; }
