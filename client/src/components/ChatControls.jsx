import { useLanguage } from '../context/LanguageContext';
export default function ChatControls({ muted, cameraOff, flipping, audioOnly, onMute, onCamera, onFlip, onNext, onEnd }) { const { t } = useLanguage(); return <div className="chat-controls"><button onClick={onMute} aria-label={t('mute')}>{muted ? t('unmute') : t('mute')}</button>{!audioOnly && <><button onClick={onCamera} aria-label={t('cameraOff')}>{cameraOff ? t('cameraOn') : t('cameraOff')}</button><button onClick={onFlip} disabled={flipping} aria-label={t('flip')}>{flipping ? t('switching') : t('flip')}</button></>}<button className="next" onClick={onNext}>{t('next')}</button><button className="end" onClick={onEnd}>{t('end')}</button></div>; }
