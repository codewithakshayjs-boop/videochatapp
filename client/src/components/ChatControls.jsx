export default function ChatControls({ muted, cameraOff, onMute, onCamera, onNext, onEnd }) { return <div className="chat-controls"><button onClick={onMute} aria-label="Toggle microphone">{muted ? '◉ Unmute' : '◉ Mute'}</button><button onClick={onCamera} aria-label="Toggle camera">{cameraOff ? '▣ Camera on' : '▣ Camera off'}</button><button className="next" onClick={onNext}>↻ Next</button><button className="end" onClick={onEnd}>End</button></div>; }
