import { useLanguage } from '../context/LanguageContext';
export default function GenderSelector({ value, onChange }) { const { t } = useLanguage(); return <div className="gender-grid">{[['male', '♂'], ['female', '♀']].map(([key, icon]) => <button type="button" onClick={() => onChange(key)} className={`gender-card ${value === key ? 'selected' : ''}`} key={key}><span>{icon}</span><strong>{t(key)}</strong></button>)}</div>; }
